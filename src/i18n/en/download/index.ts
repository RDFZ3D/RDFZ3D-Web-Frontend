import type { BaseTranslation } from "../../i18n-types";

const en_download = {
  download_version: "Download {platform:string} version",
  suggested_by_platform: "Recommanded for your platform",
  other_platforms: "other platforms",
} satisfies BaseTranslation;

export default en_download;
